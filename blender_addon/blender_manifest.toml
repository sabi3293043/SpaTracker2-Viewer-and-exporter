schema_version = "1.0.0"

id = "spatracker2_importer"
version = "1.0.0"
name = "SpaTracker2 Importer"
tagline = "Import SpaTracker2 tracking data into Blender"
maintainer = "SpaTracker2 Team"
type = "add-on"

blender_version_min = "5.0.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

tags = ["Import-Export", "3D View", "Animation"]

platforms = ["windows-x64"]

[permissions]
files = "Import NPZ tracking data files"
